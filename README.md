# gcc-list

## A linked List example in C

Implements the following functions:

* new() creates a new node.

* prepend() prepends a new node to the start of a list

* append() appends a new node to the end of a list 

* insert() inserts a new node into an existing list in order

* print() prints the contents of a list

